# SECURITY

## API 키 관리 규칙

- **`.env`에 실제 키를 커밋하지 않습니다.** `.gitignore`에서 `.env`, `.env.*`(단 `.env.example` 제외)를 이미 차단하고 있습니다.
- **새 키가 필요하면 `.env.example`에 플레이스홀더만 추가**하고, 실제 값은 팀 시크릿 관리 채널(예: 1Password, GitHub Actions Secrets)로 공유합니다.
- **코드에 키를 하드코딩하지 않습니다.** 모든 클라이언트가 `os.getenv(...)`로 읽도록 되어 있는 기존 패턴을 유지하세요.
- 이 저장소를 처음 세팅할 때 `.env` 파일에는 `여기에_API_KEY를_붙여넣으세요` 같은 플레이스홀더만 들어 있었고 실제 키가 노출된 적은 없습니다. 하지만 앞으로 실제 키를 채운 `.env`를 절대 `git add`하지 않도록 주의하세요.

## 현재 보안 경계의 한계

- **`/api/*` 엔드포인트에 X-API-Key 인증이 추가됨(2026-07-21)**, 단 `SPEAKO_API_KEY`를 실제 값으로 채워야 켜집니다. `.env`가 플레이스홀더 상태(로컬 개발 기본값)면 인증이 꺼진 채로 동작하도록 fail-open 설계되어 있습니다 — **배포 전 반드시 `SPEAKO_API_KEY`를 실제 값으로 채우세요.** 안 채우면 지금처럼 무인증 상태 그대로 배포됩니다. 구현: `main.py`의 `verify_api_key`(FastAPI `APIRouter` 의존성). `/`(헬스체크)는 인증 대상에서 제외되어 있습니다.
  - 지금은 단일 공유 비밀키 방식이라 "누가 호출했는지"는 구분 못 하고 "정당한 호출인지"만 구분합니다. 프로젝트 소유권 기반 인가(내 프로젝트만 조회 가능)는 사용자 계정 시스템이 생겨야 가능 — 아직 없음.
- **CORS가 `localhost:3000` 하나로 고정되어 있습니다.** 프로덕션 배포 시 실제 프론트엔드 도메인으로 좁혀서 설정해야 하며, `allow_origins=["*"]`로 넓히지 마세요.
- **업로드 파일 크기/타입 제한이 없습니다.** `/api/ppt/extract`, `/api/evaluation/audio` 모두 파일 크기 상한이 없어 큰 파일 업로드로 디스크를 소진시킬 수 있습니다 (DoS 벡터). *(→ 해결됨: PPT 20MB/.pptx만, 오디오 10MB/.wav만 제한 추가.)*
- ~~**업로드된 파일명을 그대로 서버 파일시스템 경로에 사용합니다**~~ → 해결됨(2026-07-21). `main.py`의 `_safe_temp_path()`가 원본 파일명 대신 확장자만 추출해 새 임의 이름(`temp_{uuid}{ext}`)을 만들도록 교체함.

## 배포 전 체크리스트

- [x] 엔드포인트 인증/인가 추가 (X-API-Key) — **단, 배포 시 `SPEAKO_API_KEY` 환경변수를 반드시 실제 값으로 설정해야 실제로 켜짐**
- [ ] CORS origin을 실제 프론트엔드 도메인으로 제한
- [x] 업로드 파일 크기 상한 설정 (PPT 20MB, 오디오 20MB + 녹음 길이 15분)
- [x] 업로드 파일명 새니타이징 (경로 조작 방지)
- [ ] 실제 배포 환경의 `.env`는 코드와 완전히 분리된 시크릿 저장소에서 주입
- [ ] (인증이 단일 공유키뿐이라) 사용자 계정/소유권 기반 인가는 별도 작업으로 필요
