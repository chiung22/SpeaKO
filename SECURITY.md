# SECURITY

## API 키 관리 규칙

- **`.env`에 실제 키를 커밋하지 않습니다.** `.gitignore`에서 `.env`, `.env.*`(단 `.env.example` 제외)를 이미 차단하고 있습니다.
- **새 키가 필요하면 `.env.example`에 플레이스홀더만 추가**하고, 실제 값은 팀 시크릿 관리 채널(예: 1Password, GitHub Actions Secrets)로 공유합니다.
- **코드에 키를 하드코딩하지 않습니다.** 모든 클라이언트가 `os.getenv(...)`로 읽도록 되어 있는 기존 패턴을 유지하세요.
- 이 저장소를 처음 세팅할 때 `.env` 파일에는 `여기에_API_KEY를_붙여넣으세요` 같은 플레이스홀더만 들어 있었고 실제 키가 노출된 적은 없습니다. 하지만 앞으로 실제 키를 채운 `.env`를 절대 `git add`하지 않도록 주의하세요.

## 현재 보안 경계의 한계

- **모든 API 엔드포인트가 무인증입니다.** `main.py`의 어떤 라우터도 인증/인가를 요구하지 않습니다. 지금 상태로 공개 인터넷에 배포하면 누구나 HyperCLOVA X/ETRI/Azure Speech/Clova Voice 호출을 트리거해 과금을 유발할 수 있습니다.
  - 최소 조치: API Gateway 레벨 키 검증 또는 FastAPI 미들웨어로 `X-API-Key` 헤더 검증을 추가하세요.
- **CORS가 `localhost:3000` 하나로 고정되어 있습니다.** 프로덕션 배포 시 실제 프론트엔드 도메인으로 좁혀서 설정해야 하며, `allow_origins=["*"]`로 넓히지 마세요.
- **업로드 파일 크기/타입 제한이 없습니다.** `/api/ppt/extract`, `/api/evaluation/audio` 모두 파일 크기 상한이 없어 큰 파일 업로드로 디스크를 소진시킬 수 있습니다 (DoS 벡터).
- **업로드된 파일명을 그대로 서버 파일시스템 경로에 사용합니다** (`temp_{uuid}_{원본파일명}`). 원본 파일명에 경로 조작 문자(`../` 등)가 섞여 들어올 가능성을 배제하려면, 파일명에서 확장자만 추출해 사용하는 것을 권장합니다.

## 배포 전 체크리스트

- [ ] 엔드포인트 인증/인가 추가
- [ ] CORS origin을 실제 프론트엔드 도메인으로 제한
- [ ] 업로드 파일 크기 상한 설정 (예: PPT 20MB, 오디오 10MB)
- [ ] 업로드 파일명 새니타이징 (경로 조작 방지)
- [ ] 실제 배포 환경의 `.env`는 코드와 완전히 분리된 시크릿 저장소에서 주입
