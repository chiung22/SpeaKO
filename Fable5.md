# Fable5.md — 이 저장소에서 일하는 하위 모델을 위한 코칭

> 이 문서는 Claude Fable 5가 SpeaKO 코드베이스를 여러 라운드에 걸쳐 작업하며 **실제로 반복해서 만난 실패 패턴**을 정리한 것입니다. 목적은 하나입니다: 이 저장소에서 일하게 될 (Fable 5든 하위 모델이든) 다음 에이전트가 **같은 실수를 다시 하지 않는 것.**
>
> 여기 적힌 규칙은 추상적인 베스트프랙티스가 아니라, 전부 이 프로젝트에서 실제로 터진 버그에서 뽑아낸 것입니다. 각 항목에 "이 프로젝트에서 실제로 이런 일이 있었다"는 근거가 붙어 있습니다. 먼저 [AGENTS.md](AGENTS.md) → [ARCHITECTURE.md](ARCHITECTURE.md) → [docs/design-docs/core-beliefs.md](docs/design-docs/core-beliefs.md) → [docs/exec-plans/tech-debt-tracker.md](docs/exec-plans/tech-debt-tracker.md)를 읽고, 그 다음 이 문서를 읽으세요.

---

## 0. 이 저장소를 건드리기 전에 (체크리스트)

1. **`pytest tests/ -q`를 먼저 돌려 현재 통과 개수를 확인**한다. 작업 후 이 숫자가 줄면 안 된다. (실행: `cd speako-ai-server && venv/Scripts/python.exe -m pytest tests/ -q`)
2. 외부 API(HCX/ETRI/Azure/Clova Voice/표준국어대사전)를 건드린다면 **키 없이도 동작하는 안전 모드(fallback)를 먼저 설계**한다. core-beliefs #1은 이 프로젝트의 헌법이다.
3. 새 기능엔 **반드시 스모크 테스트를 하나 이상** 추가한다. 그 테스트는 **실제 네트워크를 때리면 안 된다**(아래 §8).
4. 응답 형태(response shape)나 엔드포인트 경로를 바꾸면, **그걸 참조하는 모든 것**(테스트, `docs/product-specs/*`, `docs/generated/db-schema.md`, `src/_*.py` 헬퍼, `ARCHITECTURE.md`)을 같이 고친다. (§7)

---

## 1. "코드가 맞아 보인다"와 "실제로 동작한다"는 완전히 다른 것

가장 비싸게 배운 교훈. 이 저장소에서 **가장 심각했던 버그들은 전부 "코드는 그럴듯한데 실제로는 한 번도 동작한 적이 없던" 것들**이었다.

- **실제 사례 1 — 대본 생성이 한 번도 성공한 적 없었음.** `FullScriptGenerator`가 구버전 `v1` 엔드포인트를 호출하고 있어서, `HCX-005` 모델로는 매 호출이 `40084 Unsupported API` 로 실패했다. 코드는 완벽히 "맞아 보였고" 테스트도 통과했다. **실제 키로 라이브 호출을 해보기 전까지 아무도 몰랐다.**
- **실제 사례 2 — topic/outline만으로 대본 생성 시 슬라이드 유실.** 모델이 슬라이드 1개를 4개로 쪼개 응답하면, 코드가 "원본에 없는 슬라이드 번호"를 전부 버렸다. 유닛 테스트로는 안 잡히고 **실제 HCX 응답으로 돌려보고서야** 발견됐다.
- **실제 사례 3 — 장단음 카테고리가 죽은 코드.** 분류 로직이 컴파일되고 테스트도 있었지만, `is_different=True`일 때만 장단음을 확인하는 로직 오류 때문에 실전에서 거의 발동하지 않았다. 테스트가 우연히 `is_different=True` 케이스만 커버해서 통과했다.

**규칙:**
- LLM/외부 API 연동을 새로 만들거나 고쳤으면, **가능한 한 실제 라이브 호출로 최소 1회 검증**한다. mock 테스트만으로는 "엔드포인트/포맷/인증이 실제로 맞는지"를 절대 증명 못 한다.
- 라이브 호출이 유료/불가능하면, 최소한 **그 사실을 사용자에게 명시**하고 "코드상으로는 맞지만 실제 검증은 못 했다"고 정직하게 말한다. "구현했습니다"라고만 하면 안 된다.
- 검증할 땐 **정상 케이스만이 아니라 실제 모델이 낼 법한 지저분한 출력**으로 돌려본다.

---

## 2. LLM 출력은 프롬프트 스펙을 절대 그대로 지키지 않는다

- **실제 사례.** 시스템 프롬프트로 `slides[N]{slide_number,script}:` 헤더+행 구조를 강제했지만, 모델은 `slides[N]{...}`를 슬라이드마다 반복하거나, 예시 문장("수정된 대본 내용입니다")을 그대로 베끼거나, 지시한 슬라이드 개수와 다르게 응답했다. 엄격한 파서(헤더 1줄 가정 + CSV 파싱)는 이런 변형에서 통째로 깨졌다.

- **실제 사례 2 — 입력이 길수록 포맷을 버린다.** 19장짜리 PPT를 한 번에 넣었더니 모델이 TOON을 포기하고 `slides{15,script}:` 뒤에 **전체를 줄글 하나로** 써버렸다. 파서는 그 헤더를 "15번 슬라이드의 대본"으로 오인해 통과시켰고, 결과적으로 **19장짜리 발표가 1장짜리 대본으로 저장**됐다. 6장씩 나눠 요청하니 1장 → 18장으로 회복됐다.
- **실제 사례 3 — 모델은 슬라이드 번호를 항상 1번부터 다시 매긴다.** 13~18번을 보냈는데 응답은 1~8번이었다. "입력에 있던 번호를 그대로 유지하라"고 프롬프트에 써도 안 지킨다. 절대 번호를 믿고 필터링하면 그 묶음이 통째로 버려진다.
- **실제 사례 4 — 줄 수가 안 맞으면 내용이 통째로 밀린다.** 6장을 넣었는데 모델이 목차 장을 건너뛰고 다음 장을 두 줄로 쪼개서, **2번 슬라이드 대본에 3번 내용**이 들어갔다. 발표자가 목차를 띄운 채 개념을 설명하게 되는, 겉으로는 멀쩡해 보이는 오류다. "넣은 개수 == 나온 개수" 검증도 못 잡는다 — 한 장을 건너뛰고 다른 한 장을 쪼개면 개수는 그대로다. **전체의 26%가 이웃 슬라이드 내용을 말하고 있었다.** 결국 한 장씩 요청하는 것 말고는 구조적 해법이 없었다.
- **실제 사례 5 — 필요 없는 포맷을 요구해서 멀쩡한 답을 버렸다.** 한 장씩 요청하도록 바꾼 뒤에도 TOON 껍데기를 계속 요구했더니, 모델이 **내용은 완벽한 대본을 평문으로** 내놓는데 "TOON이 아니다"는 이유로 19장 중 18장을 폐기했다. 한 장만 요청하면 응답 전체가 곧 그 답이므로 구분자가 애초에 필요 없었다.
- **실제 사례 6 — 지시를 늘릴수록 나빠질 수 있다.** 포맷 준수율을 높이려고 시스템 프롬프트 규칙을 8개까지 늘렸더니 오히려 19장 중 13장이 형식을 어겼다. 규칙을 6개로 줄이고 불필요한 포맷 요구를 없애자 19/19가 됐다.
- **실제 사례 7 — 라벨로 주면 무시하고, 지시문으로 주면 지킨다.** 슬라이드를 한 장씩 독립 요청하면 모델은 자기가 발표 중간인 걸 모른다. `[위치] 3번째 슬라이드`처럼 라벨로 주면 무시하고 19장 중 16장이 "안녕하세요"로 시작했다. **"인사말로 시작하지 마세요"라고 문장으로 못박고 프롬프트 맨 뒤로 옮기자** 0장이 됐다.

**규칙:**
- LLM 출력 파서는 **관대하게** 짠다. "숫자,텍스트" 패턴 자체를 정규식으로 찾아내는 식으로, 헤더 유무·반복·순서 뒤바뀜을 견디게 한다 (`src/clova/toon_parser.py` 참고).
- 단, **관대함이 "쓰레기를 통과시키는 것"이 되면 안 된다.** 헤더/포맷 정의 문구(`script`, `slide_number`)가 데이터 행으로 둔갑하지 않는지 반드시 걸러라. 관대한 파서일수록 "무엇을 거부해야 하는가"를 명시적으로 테스트해야 한다.
- **N개 항목을 N개 결과로 매핑해야 한다면, 한 번에 하나씩 요청해라.** 묶어서 요청하는 한 정렬은 확률의 문제가 되고, 개수 검증으로도 못 잡는다. 하나씩 보내면 무슨 응답이 오든 그 항목의 답이므로 **정렬이 구조적으로 보장**된다. 비용이 몇 배 늘지만, 조용히 밀린 결과보다 낫다.
- **모델이 매긴 번호를 신뢰하지 마라.** 번호는 호출한 쪽에서 붙여라.
- **꼭 필요한 포맷만 요구해라.** 응답이 하나면 구분자가 필요 없다. 안 쓰는 껍데기를 요구하면 그걸 안 지켰다는 이유로 멀쩡한 답을 버리게 된다.
- **프롬프트 규칙은 적을수록 잘 지킨다.** 규칙이 안 지켜질 때 규칙을 더 추가하는 건 보통 역효과다. 먼저 "이 요구가 정말 필요한가"를 묻고, 남길 규칙은 **지시문 형태로 프롬프트 맨 뒤에** 둬라(라벨로 주면 무시한다).
- 파싱 실패 시엔 예외로 죽지 말고 **안전한 폴백**(`{"raw_toon": ...}`)을 반환한다.
- 프롬프트의 **예시 문장은 모델이 그대로 베낄 수 있다.** 예시엔 "실제로 나와야 할 형태의 진짜 문장"을 넣어라("여기에 대본" 같은 플레이스홀더 금지).
- 생성 슬라이드 수가 원본과 다를 수 있음을 항상 가정한다(upsert, `maxTokens` 잘림 등).

---

## 3. 안전 모드(fallback)는 "입력의 함수"여야 하고, 키를 건드리기 "전에" 가드해야 한다

core-beliefs #1을 지키는 건 기본이지만, **잘못된 fallback은 없는 것보다 나쁘다.**

- **실제 사례 1 — 가짜로 동작하는 fallback.** ETRI 키가 없을 때 `/api/analysis/words`가 **입력 대본과 무관하게 고정된 4단어**(`메타버스`,`인프라`,`특징`,`구축`)를 반환했다. 어떤 PPT를 넣어도 같은 결과 → 겉보기엔 동작하지만 실제로는 망가진 상태. → 지금은 "이 프로젝트의 실제 대본에서 빈도 기반으로 뽑는" 방식으로 고침.
- **실제 사례 2 — 키를 헤더에 넣고 나서야 죽음.** ETRI 클라이언트가 플레이스홀더 키(`여기에_...`, 한글 포함)를 그대로 `Authorization` 헤더에 넣어 요청하다가 latin-1 인코딩 에러로 크래시했다. → `use_fallback` 가드를 **네트워크 호출 전에** 둬서 해결.
- **실제 사례 3 — 패턴 불일치.** 4개 클라이언트(ETRI/Azure/Clova Voice/stdict)는 `__init__`에서 `use_fallback = not api_key or "여기에_" in api_key`를 두고 조기 반환하는데, **HCX 계열 4개는 이 가드가 없어서** 키가 없어도 무조건 네트워크를 때렸다(`Authorization: Bearer None`). 이게 CI에서 실제 HCX를 호출하던 테스트 버그의 근본 원인이었다.

**규칙:**
- fallback 결과는 **반드시 실제 입력의 함수**여야 한다. 하드코딩된 상수 반환 금지.
- 키 검증(`use_fallback`)은 **어떤 네트워크/헤더 작업보다 먼저** `__init__`에서 계산하고, 호출 메서드 맨 앞에서 조기 반환한다.
- **이미 있는 패턴을 그대로 따라라.** 새 외부 클라이언트를 추가하면 기존 클라이언트의 `use_fallback` 패턴을 복붙 수준으로 맞춰라. 나만의 다른 방식을 만들면 §3 사례 3 같은 불일치 버그가 생긴다.

---

## 4. 내가 추가한 기능이 "조용히 죽어" 있을 수 있다

- **실제 사례 1.** 장단음/연음/표기-발음불일치 3분류 기능을 추가했는데, 장단음 판정을 `is_different=True`(철자≠발음)일 때만 호출했다. 그런데 장단음(모음 길이)은 한글 철자에 드러나지 않아서 G2P의 철자≠발음 판정에 거의 안 걸린다 → **장단음 분류가 사실상 한 번도 발동 안 함.** 테스트는 우연히 `is_different=True`인 단어만 넣어서 통과했다.
- **실제 사례 2 — 안전 모드가 실패를 감춘 최악의 케이스.** HCX 비전에 이미지를 보낼 때 `dataUri.data`에 `data:image/png;base64,` 접두어를 빼고 순수 base64만 보냈다. HCX는 이걸 400(`40001`)으로 **전부** 거절했는데, 호출부는 실패하면 빈 문자열을 반환한다(안전 모드). 그래서 로그에는 "이미지에 글자가 없나 보다"와 **완전히 똑같이** 보였다. 이미지 전용 PPT 5개의 대본 추출이 조용히 실패했고, 나는 원인을 "PowerPoint의 EMF/WMF 포맷 문제"로 **잘못 진단해 사용자에게 보고까지 했다**. 실제로 확인해보니 EMF는 한 장도 없었고 전부 PNG였다. 결정적 증거는 사용량 로그였다 — 비전 호출 **성공 기록이 92건 중 0건**.

**규칙:**
- 새 분기(branch)를 만들면 **그 분기가 실제로 도달 가능한지** 스스로 반증해봐라. "이 조건이 참이 되는 실제 입력이 존재하는가?"
- 테스트는 **결정 분기 전부**를 커버해라. 특히 "아무 일도 안 일어나는" 경로(`category=None`, 빈 리스트, early-return)도 명시적으로 테스트한다.
- 두 신호가 독립적이면(예: 장단음 vs 철자-발음 불일치) **서로의 게이트로 묶지 마라.**
- **안전 모드로 삼킨 에러는 반드시 원인을 로그로 남겨라.** 특히 HTTP 4xx/5xx는 상태코드만이 아니라 **응답 본문**을 찍어야 한다. `raise_for_status()`의 예외 메시지에는 서버가 알려준 진짜 이유가 안 들어있다.
- **"결과가 비었다"와 "호출이 실패했다"를 구분할 수 있게 만들어라.** 둘이 같은 모양이면 버그가 영원히 안 보인다.
- **추측으로 원인을 단정하지 마라.** 외부 API가 의심되면 최소 재현(알려진 정상 입력 1건)을 만들어 실제 응답 본문을 봐라. 사용량/성공 로그에 "성공 0건"이 찍혀 있으면 그건 엣지 케이스가 아니라 **전면 장애**다.

---

## 5. 반복(loop) 안의 외부 호출엔 상한·캐시·타임아웃이 필수

- **실제 사례.** `/api/analysis/words`가 발음 주의 단어마다 표준국어대사전을 **2번씩 직렬 HTTP 호출**했는데, 캐시도 상한도 없었다. 대본에 고유 단어가 수백 개면 수백 번의 직렬 호출 → 무료 정부 API 폭격 + 요청이 수 분간 멈춤. (이건 Fable 5가 직접 넣은 버그다. 새 기능을 넣을 때 "단어당 외부 호출"이라는 비용 구조를 스스로 못 알아챘다.)

**규칙:**
- "N개 항목마다 외부 호출"이 보이면 즉시 세 가지를 붙여라: **(1) 개수 상한**(`MAX_DIFFICULT_WORDS=40`처럼, 초과 시 로그로 알림 — 조용히 자르지 말 것), **(2) 결과 캐시**(같은 입력 재호출 방지), **(3) 타임아웃**(이 저장소는 30초).
- 모든 `requests.post/get`에 `timeout=`을 반드시 넣는다. 이 저장소의 기존 규칙이다.

---

## 6. 실패는 정직하게, 내부는 새어나가지 않게

core-beliefs #3(실패는 HTTP 상태코드로)을 지키되, 두 가지를 더 챙겨라.

- **실제 사례 1 — 예외 메시지 누출.** `/api/evaluation/audio`가 `except Exception as e: raise HTTPException(502, f"...{str(e)}")`로 내부 예외 문자열을 그대로 클라이언트에 노출했다. → 서버 로그로만 남기고 응답은 일반 메시지로.
- **실제 사례 2 — cleanup이 진짜 에러를 덮음.** `finally`의 `os.remove()`가 Windows에서 파일 핸들이 잡혀 있으면 `PermissionError`를 내는데, 이게 이미 raise된 HTTPException(502)을 덮어써서 500으로 바꿔버렸다. → `finally`의 각 정리 작업을 개별 try/except로 감쌌다.
- **손상 입력은 422로.** 라이브러리 예외(`pypdf`/`python-docx`가 손상 파일에서 던지는 것)를 그대로 500으로 흘리지 말고 try/except로 감싸 422로 정직하게 알린다.

**규칙:**
- 외부 라이브러리 호출(파일 파싱 등)은 **가능한 실패를 예상하고** try/except로 감싸 의미 있는 4xx로 변환한다.
- `except Exception as e`에서 **`str(e)`를 응답 body에 넣지 마라.** 로그로만.
- `finally`의 정리 코드는 절대 예외를 밖으로 던지면 안 된다(이미 처리된 에러를 덮는다).

---

## 7. 무언가를 바꾸면, 그걸 "참조하는 모든 것"을 같이 바꿔라

- **실제 사례.** `/api/ppt/extract`를 `/api/projects`로 바꿨는데, `src/_db_smoke_test.py`는 여전히 옛 엔드포인트를 호출해서 즉시 404로 죽는 죽은 코드가 됐다. `docs/product-specs/*`도 옛 엔드포인트·옛 `feedback` 필드·"대본이 저장되지 않습니다"(이미 DB 생김) 같은 사실과 어긋난 내용이 오래 방치됐다.

**규칙:**
- 엔드포인트/응답형태/함수시그니처를 바꾸면 `grep`으로 **전체 저장소에서 참조를 찾아** 같이 고친다: 테스트, `src/_*.py` 헬퍼 스크립트, `docs/product-specs/`, `docs/generated/db-schema.md`, `ARCHITECTURE.md`.
- 문서는 코드와 함께 낡는다. 코드를 고쳤으면 관련 문서의 "현재 상태"/"알려진 제약"을 반드시 재확인한다.

---

## 8. 테스트는 격리되어야 한다 (네트워크·DB·실제 키)

- **실제 사례 1 — CI에서 실제 유료 API 호출.** `test_..._fails_without_api_key` 두 개가 `api_key=None`만 설정하고 `requests.post`를 mock 안 해서, CI에서 실제 HyperCLOVA X를 `Authorization: Bearer None`으로 호출하고 있었다. (GitHub Actions 결제 이슈까지 겹친 저장소에서 특히 나쁨.)
- **실제 사례 2 — 로컬 .env의 실제 키가 테스트에 샘.** `/api/analysis/words` 테스트가 로컬에 있는 실제 `STDICT_API_KEY`로 진짜 사전 API를 호출하게 됐다. → conftest에 `stdict_client.use_fallback = True`를 autouse로 강제해 해결.

**규칙:**
- 테스트는 **경계(boundary)에서 mock**한다: `requests.post`를 monkeypatch하거나, 클라이언트의 `use_fallback` 플래그를 강제한다.
- **CI엔 `.env`가 없다.** 즉 CI에선 모든 키가 미설정 → `use_fallback=True`. 그러므로 "mock된 네트워크 경로"를 타는 테스트는 `use_fallback=False`를 명시적으로 강제해야 하고("이 테스트는 파싱 검증이지 키 검증이 아니다"), "키 없음"을 검증하는 테스트는 `use_fallback=True`를 강제해라.
- 실제 키가 로컬 `.env`에 있을 수 있다는 걸 항상 가정하고, conftest의 autouse fixture로 외부 호출을 원천 차단한다(`_isolate_usage_log`, `_isolate_stdict`, 인메모리 DB override 참고).

---

## 9. 환경·플랫폼 현실을 무시하지 마라 (이 저장소는 Windows다)

- **실제 사례들.** ① Windows 콘솔 기본 코드페이지(cp949)가 이모지를 인코딩 못 해 `print()`에서 서버가 부팅 중 죽었다(→ `main.py` 최상단 `sys.stdout.reconfigure(utf-8)`). ② `g2pkk`가 Windows에서 로드 실패 → 자체 fallback 사전으로 전환. ③ 오디오 변환엔 시스템에 `ffmpeg`가 깔려 있어야 함. ④ Windows에선 파일 핸들이 잡혀 있으면 `os.remove`가 실패.

**규칙:**
- 파일 삭제, 서브프로세스, 서드파티 라이브러리 로드는 **Windows에서 다르게 동작할 수 있음**을 가정하고 방어한다.
- 새 시스템 의존성(ffmpeg 같은 exe)이 필요하면 **사용자/서버 담당자에게 설치 가능 여부를 먼저 확인**한다(임의로 도입하지 말 것).

---

## 10. FastAPI에서 `async def` + 블로킹 I/O는 함정

- **현재 상태(미수정, tech-debt에 기록됨).** 이 저장소의 모든 핸들러가 `async def`인데 내부에서 `requests.post`, `subprocess.run`, Azure SDK의 `done.wait(timeout=300)` 같은 **동기 블로킹 호출**을 그대로 한다. FastAPI는 `async def` 라우트를 스레드풀로 오프로드하지 않으므로, 단일 워커에서 한 요청이 이벤트루프를 최대 5분간 잡으면 다른 모든 요청이 멈춘다. 지금은 단일 사용자라 안 드러나지만 **배포 전 반드시 처리**해야 한다.

**규칙:**
- 블로킹 I/O만 하는 핸들러는 **`def`로 선언**(FastAPI가 스레드풀로 돌려줌)하거나 `run_in_threadpool`로 감싼다.
- 프론트/배포 관련 작업을 할 땐 tech-debt-tracker의 "배포 시" 항목들(이벤트루프, 업로드 본문 크기, 레이트리밋)을 먼저 확인한다.

---

## 11. 일하는 방식에 대한 메타 코칭

- **"~할 예정입니다"로 끝내지 마라.** 고칠 수 있으면 지금 고친다. 막혔으면(키·설계 결정) 무엇이 왜 막혔는지 명확히 말하고 멈춘다.
- **되돌리기 어려운 작업(레포 공개 전환, 파일 삭제, force push)은 하기 전에 검증**한다. 예: 레포를 public으로 바꾸기 전 git 히스토리 전체에서 커밋된 시크릿을 먼저 검사했다.
- **작업이 끝나면 `pytest`로 회귀 없음을 확인**하고, 통과 개수를 사용자에게 보고한다. 못 돌렸으면 "못 돌렸다"고 정직하게 말한다.
- **다각도 검토가 필요한 큰 작업은 서브에이전트를 병렬로** 돌려라(정확성/보안/테스트 등 관점 분리). 단, 사용자가 명시적으로 요청했을 때만. 이 문서 자체가 그 방식으로 나온 검토 결과다.
- **모든 진행 상황은 `nextStep.md`에 기록**한다. 다음 세션(다른 모델일 수 있음)이 맥락 없이 이어받을 수 있게.

---

## 부록: 이 저장소의 핵심 파일 지도

| 파일 | 역할 |
|---|---|
| `speako-ai-server/src/main.py` | 모든 API 엔드포인트. 여기가 대부분의 버그가 살던 곳 |
| `src/clova/full_generation/generator.py` | 대본 생성 + 어투 고도화 (HCX). `use_fallback` 패턴 참고 |
| `src/clova/toon_parser.py` | LLM 출력을 관대하게 파싱하는 공용 파서 (§2) |
| `src/utils/stdict_client.py` | 외부 API 클라이언트 + 캐시 패턴 참고 (§5) |
| `src/db/database.py`, `models.py` | SQLite + SQLAlchemy. 마이그레이션 도구 없음(스키마 바꾸면 dev DB 삭제) |
| `tests/conftest.py` | 테스트 격리(DB·usage log·stdict) 패턴 (§8) |
| `docs/exec-plans/tech-debt-tracker.md` | 알려진 미해결 항목. 새 이슈는 여기 먼저 기록 |
| `docs/design-docs/core-beliefs.md` | 절대 원칙 3가지 (안전모드/토큰절약/정직한 상태코드) |
| `nextStep.md` | 세션 간 인수인계 로그 |
