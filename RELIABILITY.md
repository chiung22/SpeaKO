# RELIABILITY

시스템 안정성, 예외 처리, 장애 대응 전략입니다.

## 이미 잘 되어 있는 것

- **5개 외부 AI 클라이언트 전부 안전 모드(fallback/mock)를 갖추고 있습니다.** 키가 없거나(`"여기에_" in key`로 감지) 호출이 실패하면 서버를 죽이지 않고 모의 데이터를 반환합니다. 이 패턴은 장애 격리(fault isolation)의 좋은 예시이며, 새 연동을 추가할 때도 반드시 유지해야 합니다.
- **PPT 추출/대본 생성 실패는 HTTP 상태코드로 구분됩니다** (`422`, `502`). 프론트엔드가 실패를 감지할 수 있습니다.
- **임시 파일은 `uuid`로 고유화되어 있고 `finally`에서 정리됩니다** — 동시 요청 간 파일 충돌이나 디스크 누적을 막습니다.

## 알려진 약점

- **재시도/타임아웃 정책이 없습니다.** `requests.post(...)` 호출들이 타임아웃 없이 무한 대기할 수 있습니다. 외부 API가 느려지면 해당 요청 워커가 계속 잡혀 있게 됩니다.
- **서킷 브레이커/레이트리밋이 없습니다.** 외부 API가 반복적으로 실패해도 매 요청마다 다시 호출을 시도합니다.
- **임시 파일이 로컬 디스크에 저장됩니다.** 서버를 여러 인스턴스로 수평 확장하면 문제가 없지만(요청 내에서만 파일을 쓰고 지우므로), 디스크 I/O 장애 시 파일 쓰기가 실패하는 케이스에 대한 처리가 없습니다.
- **`/api/evaluation/audio`는 실패해도 200을 반환합니다.** 바디의 `status` 필드로만 구분되어, 위 문서들의 "실패는 상태코드로" 원칙과 일관되지 않습니다.
- **구조화된 로깅이 없습니다.** 전부 `print()`이며 요청 추적 ID가 없어, 운영 중 특정 요청의 실패 원인을 로그만으로 추적하기 어렵습니다.

각 항목의 해결 계획은 [docs/exec-plans/tech-debt-tracker.md](docs/exec-plans/tech-debt-tracker.md)에서 추적합니다.
