# 스프링 수정 목록 — 파일별 · 붙여넣기용

> **2026-08-17 07:38에 EC2에 배포된 JAR**을 `javap`으로 뜯어 확인했습니다.
> 추측이 아니라 배포된 바이트코드에서 읽은 값입니다.
> AI 서버 쪽은 전부 동작 확인이 끝났습니다(프로덕션 스모크 9/9 통과).
>
> 각 항목은 **그대로 붙여넣을 수 있는 단위**로 적었습니다. 새로 만드는 코드는 전체를,
> 고치는 코드는 `현재` → `바꿔주세요` 형태로 뒀습니다.

---

## 🚨 먼저 — 없는 주소를 부르고 있습니다

새로 붙이신 기능 셋이 **AI 서버에 존재하지 않는 엔드포인트**를 호출합니다. 지금 실행하면 **전부 404**입니다.

| 부르고 있는 주소 | 실제 주소 | 파일 |
|---|---|---|
| `/api/project/create-custom` | `/api/projects` | `EvaluationService`, `PresentationService` |
| `/api/tts/synthesize` | `/api/tts/word` | `AudioService` |
| `/api/project/{id}/highlights` | `/api/analysis/words` | `PresentationService` |

**TTS는 응답 형태도 다릅니다.** `audio_url` / `duration`을 꺼내고 계신데 그런 필드가 없습니다 — MP3 바이트가 그대로 옵니다.

---

## 작업 순서

| 순서 | 무엇 | 예상 |
|---|---|---|
| 0 | **§3-1 `ALTER TABLE` 실행** — 자동 생성 안 됩니다 | 5분 |
| 1 | `AI_BASE_URL` 3곳 + `aiEvaluationId` 필드·저장 | 30분 |
| 2 | 없는 주소 3개 교체 | 1~2시간 |
| 3 | 하이라이팅 저장 로직 | 반나절 |
| 4 | 상세 피드백 + 습관 지표 | 1~2시간 |
| 5 | 썸네일 | 30분 |
| 6 | 다운로드 | 30분 |

**1번이 제일 급합니다.** 1줄 수정이 데모 당일 사고를 막고, `aiEvaluationId`는 지금 저장을 시작해야 나중에 쓸 데이터가 쌓입니다.

---
---

# 1. `EvaluationService.java`

## 1-1. `AI_BASE_URL` 기본값 ⚠️ 최우선

```java
// 현재
private static final String AI_BASE_URL =
        System.getenv().getOrDefault("AI_BASE_URL",
                "https://losing-charity-floyd-transparent.trycloudflare.com");

// 바꿔주세요
private static final String AI_BASE_URL =
        System.getenv().getOrDefault("AI_BASE_URL", "http://localhost:8000");
```

저 Cloudflare 주소는 **DNS 해석조차 안 됩니다**(확인함). 지금은 EC2 환경변수가 덮고 있지만, **환경변수 없이 재시작되면 AI 연동이 통째로 조용히 실패합니다.**

## 1-2. `generateAiProjectIdForCustom()` — 메서드 통째로 교체

주소도 파라미터도 다릅니다. 아래로 바꿔주세요.

```java
/**
 * 커스텀 대본(파일 또는 수기 텍스트)을 AI 서버에 등록하고 project_id를 받아온다.
 * ⚠️ 별도 엔드포인트가 없다. PPT 업로드와 같은 POST /api/projects를 쓴다.
 */
private Long generateAiProjectIdForCustom(String scriptText, MultipartFile scriptFile) {
    RestTemplate restTemplate = new RestTemplate();
    SimpleClientHttpRequestFactory factory = new SimpleClientHttpRequestFactory();
    factory.setConnectTimeout(10_000);
    factory.setReadTimeout(30_000);
    restTemplate.setRequestFactory(factory);

    HttpHeaders headers = new HttpHeaders();
    headers.setContentType(MediaType.MULTIPART_FORM_DATA);
    addApiKeyHeader(headers);

    MultiValueMap<String, Object> body = new LinkedMultiValueMap<>();

    if (scriptFile != null && !scriptFile.isEmpty()) {
        // 완성된 대본 파일(docx/txt/pdf)
        final byte[] fileBytes;
        try {
            fileBytes = scriptFile.getBytes();
        } catch (IOException e) {
            throw new RuntimeException("대본 파일 읽기 실패", e);
        }
        final String filename = scriptFile.getOriginalFilename() != null
                ? scriptFile.getOriginalFilename() : "script.txt";

        body.add("file", new ByteArrayResource(fileBytes) {
            @Override public String getFilename() { return filename; }
        });
        // ⚠️ 이게 없으면 발표자료(PPT)로 보고 슬라이드를 뽑으려 한다
        body.add("mode", "coaching");

    } else if (scriptText != null && !scriptText.isBlank()) {
        // 수기 텍스트  ⚠️ 파라미터 이름이 "text"가 아니라 "script_text"
        body.add("script_text", scriptText);

    } else {
        throw new IllegalArgumentException("대본 파일이나 텍스트 중 하나는 반드시 존재해야 합니다.");
    }

    try {
        ResponseEntity<Map> response = restTemplate.postForEntity(
                AI_BASE_URL + "/api/projects",           // ⚠️ /api/project/create-custom 아님
                new HttpEntity<>(body, headers),
                Map.class);

        Map responseBody = response.getBody();
        if (responseBody == null || !(responseBody.get("project_id") instanceof Number)) {
            throw new IllegalStateException("AI 서버 응답에 project_id가 없습니다: " + responseBody);
        }
        return ((Number) responseBody.get("project_id")).longValue();

    } catch (HttpStatusCodeException e) {
        throw new ResponseStatusException(e.getStatusCode(),
                "AI 서버 프로젝트 생성 실패: " + extractDetail(e.getResponseBodyAsString()));
    }
}
```

**응답은 PPT 업로드와 완전히 같습니다.**
```jsonc
{ "success": true, "project_id": 16,
  "data": { "metadata": {...}, "slides": [ { "slide_number": 1, "content": "..." } ] } }
```
대본만 올린 경우 **슬라이드 1번에 전체 대본이 한 덩어리**로 들어갑니다.

## 1-3. `evaluateVoice()` — `evaluation_id` 저장 추가

`Evaluation.builder()` 부분에 한 줄만 넣으시면 됩니다.

```java
Evaluation evaluation = Evaluation.builder()
        .userId(userId)
        .presentationId(presentation.getPresentationId())
        .aiEvaluationId(aiResult.getEvaluationId())   // ← 추가 (DTO에 이미 매핑돼 있음)
        .recordingId(savedRecording != null ? savedRecording.getRecordingId() : null)
        // ... 나머지 기존 그대로
        .build();
```

**왜 오늘 해야 하나.** 상세 피드백(1-4)을 부르려면 이 번호가 필요합니다. 지금 저장을 안 하면 **오늘부터 쌓이는 평가도 전부 피드백을 못 받습니다.** 구현은 나중에 하셔도 되지만 저장만은 먼저 시작해두셔야 합니다.

## 1-4. 상세 피드백 호출 — 메서드 새로 추가

`Evaluation.feedbackDetail`이 항상 비어 있는 이유입니다. **부르는 곳이 아예 없습니다.**

**음성 파일을 다시 보낼 필요 없습니다.** 평가할 때 단어별 점수를 저장해뒀기 때문에 평가 번호 하나면 됩니다.

```java
/**
 * AI 서버에 상세 피드백을 요청해 Evaluation에 저장한다.
 * 바디 없음. 응답 6~7초(실측). cached=true면 이전 결과라 추가 요금이 없다.
 */
@Transactional
public Map<String, Object> generateFeedback(Long evaluationId) {
    Evaluation evaluation = evaluationRepository.findById(evaluationId)
            .orElseThrow(() -> new IllegalArgumentException("평가 결과를 찾을 수 없습니다."));

    Long aiEvaluationId = evaluation.getAiEvaluationId();
    if (aiEvaluationId == null) {
        throw new IllegalStateException(
                "이 평가에는 AI 평가 번호가 없습니다. (aiEvaluationId 도입 전 데이터 — 다시 녹음해주세요)");
    }

    RestTemplate restTemplate = new RestTemplate();
    SimpleClientHttpRequestFactory factory = new SimpleClientHttpRequestFactory();
    factory.setConnectTimeout(10_000);
    factory.setReadTimeout(120_000);          // 피드백 생성은 6~7초지만 넉넉히
    restTemplate.setRequestFactory(factory);

    HttpHeaders headers = new HttpHeaders();
    headers.setContentType(MediaType.APPLICATION_JSON);
    addApiKeyHeader(headers);

    Map<String, Object> data;
    try {
        ResponseEntity<Map> res = restTemplate.postForEntity(
                AI_BASE_URL + "/api/evaluation/" + aiEvaluationId + "/feedback",
                new HttpEntity<>(headers),     // 바디 없음
                Map.class);
        Map body = res.getBody();
        if (body == null || body.get("data") == null) {
            throw new IllegalStateException("AI 서버가 피드백을 주지 않았습니다: " + body);
        }
        data = (Map<String, Object>) body.get("data");

    } catch (HttpStatusCodeException e) {
        throw new ResponseStatusException(e.getStatusCode(),
                extractDetail(e.getResponseBodyAsString()));
    }

    try {
        evaluation.updateFeedbackDetail(objectMapper.writeValueAsString(data));
        evaluationRepository.save(evaluation);
    } catch (Exception e) {
        throw new RuntimeException("피드백 저장 실패", e);
    }
    return data;
}
```

**응답 예시:**
```jsonc
{ "success": true, "evaluation_id": 12, "cached": false,
  "data": {
    "summary": "총평 2~3문장",
    "strengths": ["잘한 점 1~3개"],
    "improvements": ["개선할 점 1~3개"],
    "practice_tips": [ { "key": "consonant", "title": "명확한 자음", "description": "..." } ],
    "deductions": { "factors": [
        { "label": "긴 멈춤", "message": "0.7초 이상 말이 끊긴 구간이 14번 있었습니다(가장 긴 구간 2.54초)." },
        { "label": "부정확한 발음", "message": "33개 단어가 정확도 70점 미만이었습니다(말한 단어의 6.7%)." }
    ] } } }
```

`deductions.factors[].message`는 **"왜 깎였는지"를 서버가 완성된 문장으로 써 둔 값**입니다. 화면에 그대로 띄우시면 됩니다.

---
---

# 2. `AudioService.java`

## 2-1. `AI_BASE_URL` 기본값 — 1-1과 동일하게

## 2-2. TTS 호출 — 주소·파라미터·응답이 **전부** 다릅니다

```java
// 현재
AI_BASE_URL + "/api/tts/synthesize"
body:  { "text": ..., "voice_style": ..., "speed": ... }
응답에서 "audio_url", "duration" 을 꺼내는 중   ← 그런 필드가 없습니다
```

**응답이 JSON이 아니라 MP3 바이트입니다.** 아래로 바꿔주세요.

```java
/**
 * 단어 하나의 표준 발음을 MP3로 받아 그대로 돌려준다.
 * ⚠️ 응답은 JSON이 아니라 audio/mpeg 바이트다. audio_url도 duration도 없다.
 */
public byte[] synthesizeWord(Long presentationId, String word, String voice, Integer speed) {
    Presentation p = presentationRepository.findById(presentationId)
            .orElseThrow(() -> new IllegalArgumentException("존재하지 않는 발표 자료입니다."));

    if (word == null || word.isBlank()) {
        throw new IllegalArgumentException("음성으로 변환할 단어 텍스트가 없습니다.");
    }

    RestTemplate restTemplate = new RestTemplate();
    SimpleClientHttpRequestFactory factory = new SimpleClientHttpRequestFactory();
    factory.setConnectTimeout(10_000);
    factory.setReadTimeout(30_000);
    restTemplate.setRequestFactory(factory);

    HttpHeaders headers = new HttpHeaders();
    headers.setContentType(MediaType.APPLICATION_JSON);
    addApiKeyHeader(headers);

    Map<String, Object> body = new HashMap<>();
    body.put("word", word);
    body.put("project_id", p.getAiProjectId());   // 저장된 발음기호를 찾아 쓴다 (더 정확)
    if (voice != null && !voice.isBlank()) body.put("voice", voice);
    if (speed != null) body.put("speed", speed);

    try {
        ResponseEntity<byte[]> res = restTemplate.postForEntity(
                AI_BASE_URL + "/api/tts/word",        // ⚠️ /api/tts/synthesize 아님
                new HttpEntity<>(body, headers),
                byte[].class);
        return res.getBody();

    } catch (HttpStatusCodeException e) {
        throw new ResponseStatusException(e.getStatusCode(),
                "AI 음성 합성 실패: " + extractDetail(e.getResponseBodyAsString()));
    }
}
```

**컨트롤러에서는 이렇게 내보내면 됩니다.**

```java
@PostMapping("/api/presentations/{presentationId}/tts/word")
public ResponseEntity<byte[]> ttsWord(@PathVariable Long presentationId,
                                      @RequestBody Map<String, Object> req) {
    byte[] mp3 = audioService.synthesizeWord(
            presentationId,
            (String) req.get("word"),
            (String) req.get("voice"),
            req.get("speed") == null ? null : ((Number) req.get("speed")).intValue());

    return ResponseEntity.ok()
            .contentType(MediaType.valueOf("audio/mpeg"))
            .body(mp3);
}
```

**화자 목록도 중계해주세요.** 프론트가 하드코딩하지 않게 하려고 만든 API입니다.

```java
@GetMapping("/api/tts/voices")
// → AI: GET /api/tts/voices
// { "voices": [ {"name":"동현","gender":"남성"}, ... ], "speed": {"min":-5,"max":5,"default":0} }
```

화자는 **동현·대성·혜리·고은 4명**, 속도는 **-5 ~ +5**(음수가 빠름)입니다.

> **전체 대본을 읽어주는 API는 없습니다.** 단어 단위만 있습니다. Clova Voice가 한국어 음운 규칙을 일부만 적용해서, 철자를 그대로 넣으면 틀린 발음이 나가기 때문입니다(`각자`→[각자], `책임`→[책임]). 그래서 서버가 G2P로 만든 발음 문자열을 단어마다 넣습니다.

---
---

# 3. `Evaluation.java` (엔티티)

> 🚨 **§3-1의 `ALTER TABLE`을 먼저 실행하세요.** `ddl-auto`가 `update`가 아니라 컬럼이 자동으로 생기지 않습니다. 순서를 바꾸면 앱이 안 뜨거나(`validate`) 저장 때 터집니다(`none`).

필드 하나를 추가합니다. 어노테이션은 없어도 됩니다 — Spring Boot 기본 네이밍이 `ai_evaluation_id`로 바꿔줍니다.

```java
private Long aiEvaluationId;      // AI 서버가 발급한 평가 번호. 상세 피드백 호출에 필요
```

**업데이트 메서드도 반드시 함께.** `@Getter`만 있고 세터가 없어서, 이게 없으면 상세 피드백을 채울 방법이 없습니다 — 피드백은 평가를 저장한 **뒤에** 따로 받아 덮어쓰는 흐름입니다(§1-4).

```java
public void updateFeedbackDetail(String feedbackDetail) {
    this.feedbackDetail = feedbackDetail;
}
```

`highlightDetail`도 나중에 채운다면 같은 방식으로 하나 더 만드세요.

**나머지 필드는 엔티티에 이미 있습니다** — `feedbackDetail`, `highlightDetail`, `fillerWordCount`, `fillerWordDetail`, `pauseScore`, `pauseDetail`. **값만 안 채워지고 있습니다.**

DB 컬럼도 `ai_evaluation_id` 하나만 없습니다. §3-1의 ALTER를 **먼저** 돌리세요.

---
---

# 3-1. MySQL 스키마 (`speako`)

> **`ddl-auto`는 `update`가 아닙니다** — 컬럼이 자동으로 생기지 않으므로 **아래 SQL을 직접 실행해야 합니다.**
>
> ⚠️ 이 절은 원래 2026-08-14 덤프로 썼는데, **그 덤프가 최신이 아니었습니다.** 컬럼 유무는 아래 진단 쿼리로 실제 DB를 확인한 값이 우선입니다.

## 🚨 먼저 실행 — 엔티티 고치기 전에

```sql
USE speako;

ALTER TABLE evaluations
  ADD COLUMN ai_evaluation_id BIGINT NULL COMMENT 'AI 서버가 발급한 평가 번호';
```

**순서가 중요합니다.** SQL을 먼저 돌리고 엔티티에 필드를 추가하세요.

| `ddl-auto` | ALTER 없이 필드만 추가하면 |
|---|---|
| `validate` | **앱이 아예 안 뜹니다** — `Schema-validation: missing column [ai_evaluation_id]` |
| `none` | 뜨긴 하는데 평가 저장 때 `Unknown column 'ai_evaluation_id' in 'field list'` |

> `@Column(columnDefinition = "json")`은 **Hibernate가 테이블을 만들 때만** 쓰는 값이라 ALTER를 대신하지 못합니다.
>
> **`NULL` 허용**으로 두세요. 기존 평가 기록에는 이 값이 없습니다.
>
> `highlight_detail`은 **이미 있는 것으로 확인됐습니다**(2026-08-17). 없다면 `ADD COLUMN highlight_detail JSON NULL`을 같이 넣으세요 — 엔티티가 `columnDefinition = "json"`이고 `filler_word_detail`·`pause_detail`도 `json`입니다.

## 진단 쿼리 — 작업 전에 한 번

```sql
SELECT TABLE_NAME, COLUMN_NAME, COLUMN_TYPE, IS_NULLABLE
FROM information_schema.COLUMNS
WHERE TABLE_SCHEMA = 'speako'
  AND TABLE_NAME IN ('evaluations','pronunciation_highlights','presentations')
ORDER BY TABLE_NAME, ORDINAL_POSITION;

SELECT COUNT(*) AS highlights_rows FROM pronunciation_highlights;
SELECT COUNT(*) AS ai_project_id_채워진_건수
FROM presentations WHERE ai_project_id IS NOT NULL;
```

**마지막 줄이 제일 중요합니다.** `ai_project_id`가 전부 NULL이면 AI 서버를 아예 못 부르는 상태라, `ai_evaluation_id`를 넣어도 채워질 값이 없습니다 (§1-2 참고).

`highlights_rows`가 0이 아니면 §5-3은 이미 되고 있다는 뜻입니다.

## `evaluations` — 엔티티와 DB 대조

| 엔티티 필드 | DB 컬럼 | 상태 |
|---|---|---|
| `aiEvaluationId` | — | ❌ 없음. 위 ALTER + §3에서 엔티티에 추가 |
| `highlightDetail` | `highlight_detail` | ✅ 있음 (2026-08-17 확인) |
| `presentationId` | `slide_id` | ✅ `@Column(name = "slide_id")`로 매핑됨 (2026-08-17 확인) |

### `slide_id` — 확인 완료

DB는 **슬라이드 단위**로 평가를 저장하는데 엔티티 필드명은 `presentationId`입니다. 이름은 다르지만 매핑이 들어가 있어 정상입니다.

```java
@Column(name = "slide_id", nullable = false)
private Long presentationId;
```

⚠️ **이 어노테이션을 지우지 마세요.** 없으면 Hibernate가 `presentation_id`를 찾고, 그런 컬럼은 없어서 평가 저장이 전부 실패합니다 (`Unknown column 'presentation_id' in 'field list'`).

### ⚠️ NOT NULL인데 `@PrePersist` 기본값이 없는 필드

`@PrePersist`가 `pauseScore`·`fillerWordCount`·`*_detail`은 챙기는데, 아래 넷은 비어 있습니다. AI 응답에서 하나라도 빠지면 INSERT가 통째로 실패합니다.

| 필드 | DB |
|---|---|
| `audioFileName` | `varchar(255) NOT NULL` |
| `audioDuration` | `int NOT NULL` |
| `totalScore` | `float NOT NULL` |
| `pronunciationScore` | `float NOT NULL` |

AI 서버는 넷 다 항상 보내지만, 방어적으로 두시려면:

```java
if (this.audioDuration == null)       this.audioDuration = 0;
if (this.totalScore == null)          this.totalScore = 0f;
if (this.pronunciationScore == null)  this.pronunciationScore = 0f;
```

`audioFileName`은 기본값이 의미 없으니 저장 전에 걸러내는 편이 낫습니다.

## 💡 `pronunciation_highlights` — 테이블은 이미 있습니다

이 테이블은 AI 서버가 주는 발음 주의 단어와 **필드가 1:1로 맞습니다.** 새로 만들 것이 없습니다.

> 위 진단 쿼리의 `highlights_rows`가 **0이면 §5-3을 해야 하고, 0이 아니면 이미 되고 있는 것**입니다.

```sql
script_id, word, standard_pronunciation,
category enum('mismatch','liaison','length'),
rule_desc, position_start, position_end
```

`GET /api/projects/{id}` 응답의 `difficult_words[]`와 매핑:

| DB 컬럼 | AI 응답 | 변환 |
|---|---|---|
| `word` | `word` | 그대로 |
| `standard_pronunciation` | `standard_pronunciation` | 그대로 |
| `category` | `category_code` | **`.toLowerCase()`** — DB enum이 소문자 |
| `rule_desc` | `rule_desc` | 그대로 |
| `position_start` / `position_end` | `occurrences[].position_start` / `_end` | 그대로 |
| `script_id` | `occurrences[].slide_number` | slide_number → 그 슬라이드의 `script_id`로 변환 |

**`occurrences`는 배열입니다.** 같은 단어가 여러 번 나오면 **행이 여러 개** 생깁니다 (실측: 단어 16개 → 149행). 오프셋은 **해당 슬라이드 대본 안에서의 위치**이지 전체 대본 기준이 아닙니다. 자세한 내용은 §5-3.

## 참고 — `recognized_text`에 섞여 있던 "Slide"는 고쳤습니다

예전 평가 기록의 인식 텍스트에 `Slide`가 반복해서 끼어 있었습니다. AI 서버가 채점 기준 텍스트에 `"Slide 1: "` 라벨을 붙여 넘기던 버그였고 **2026-08-16에 수정·배포**했습니다. 지금 다시 평가하면 나오지 않습니다.

`recognized_text`에 `Slide`나 숫자가 섞여 있는 기록은 **08-16 이전 데이터**입니다. 시연에 쓰지 마세요.

---
---

# 4. `AiEvaluationResponseDto.java`

`speech_metrics`를 받을 필드가 없어 `fillerWordCount` / `pauseScore` 컬럼이 **항상 null**입니다.

시연 대사에 *"발표 속도나 음성 관련 지표를 통해 말하기 습관도 확인할 수 있습니다"* 가 있어서, 안 채우면 **화면에 없는 걸 말하게 됩니다.**

```java
@JsonProperty("speech_metrics")
private SpeechMetrics speechMetrics;

@JsonProperty("deductions")
private Map<String, Object> deductions;

@JsonProperty("highlights")
private Map<String, Object> highlights;   // 선택 — words_detail로도 그릴 수 있음

@Getter @Setter @NoArgsConstructor
public static class SpeechMetrics {
    @JsonProperty("filler_words")    private Map<String, Object> fillerWords;
    @JsonProperty("pauses")          private Map<String, Object> pauses;
    @JsonProperty("speech_rate_wpm") private Integer speechRateWpm;
}
```

**AI 서버가 보내는 실제 값** (5분 녹음 실측):
```jsonc
"speech_metrics": {
  "filler_words": { "count": 0, "by_word": [], "spoken_word_count": 491, "ratio_percent": 0.0 },
  "pauses": { "count": 14, "total_seconds": 18.68, "longest_seconds": 2.54,
              "threshold_seconds": 0.7,
              "items": [ { "after_word": "하겠습니다", "seconds": 1.2 } ] },
  "speech_rate_wpm": 96
}
```

엔티티에 넣으실 때:
```java
.fillerWordCount((Integer) speechMetrics.getFillerWords().get("count"))
.fillerWordDetail(objectMapper.writeValueAsString(speechMetrics.getFillerWords()))
.pauseDetail(objectMapper.writeValueAsString(speechMetrics.getPauses()))
```

> `Grades` 내부 클래스는 **지우셔도 됩니다.** AI 서버가 등급(A~F)을 없애고 0~100 정수만 보냅니다. 점수는 `Float`로 받고 계시니 그대로 들어갑니다.

---
---

# 5. `PresentationService.java`

## 5-1. `AI_BASE_URL` 기본값 — 1-1과 동일하게 (**여기도 있습니다**)

## 5-2. 커스텀 대본 등록 — 1-2와 동일하게

## 5-3. `requestAndSaveHighlights()` — 메서드 통째로 교체

주소가 없고, 카테고리 변환도 항상 실패합니다.

```java
// 현재 — 없는 주소입니다
GET AI_BASE_URL + "/api/project/" + aiProjectId + "/highlights"
```

**두 단계로 나뉩니다.**

```java
/**
 * AI 서버에 발음 주의 단어 분석을 요청하고 하이라이트를 저장한다.
 * ① POST /api/analysis/words 로 분석 실행
 * ② 응답의 data.words 를 그대로 저장 (GET /api/projects/{id} 의 difficult_words 와 같은 값)
 */
@SuppressWarnings("unchecked")
private void requestAndSaveHighlights(Long aiProjectId, Map<Integer, Script> scriptsBySlideOrder) {
    RestTemplate restTemplate = new RestTemplate();
    SimpleClientHttpRequestFactory factory = new SimpleClientHttpRequestFactory();
    factory.setConnectTimeout(10_000);
    factory.setReadTimeout(120_000);        // 사전 조회가 있어 10초 안팎 걸린다
    restTemplate.setRequestFactory(factory);

    HttpHeaders headers = new HttpHeaders();
    headers.setContentType(MediaType.APPLICATION_JSON);
    addApiKeyHeader(headers);

    Map<String, Object> reqBody = new HashMap<>();
    reqBody.put("project_id", aiProjectId);

    Map responseBody;
    try {
        responseBody = restTemplate.postForEntity(
                AI_BASE_URL + "/api/analysis/words",      // ⚠️ 주소 변경
                new HttpEntity<>(reqBody, headers),
                Map.class).getBody();
    } catch (HttpStatusCodeException e) {
        throw new ResponseStatusException(e.getStatusCode(),
                extractDetail(e.getResponseBodyAsString()));
    }

    Map<String, Object> data = responseBody == null
            ? null : (Map<String, Object>) responseBody.get("data");
    if (data == null || data.get("words") == null) {
        throw new IllegalStateException("AI 서버가 분석 결과를 주지 않았습니다: " + responseBody);
    }

    for (Map<String, Object> w : (List<Map<String, Object>>) data.get("words")) {

        // ⚠️ category(한국어)가 아니라 category_code(영문)를 쓴다. 아래 설명 참고.
        HighlightCategory category;
        try {
            category = HighlightCategory.valueOf((String) w.get("category_code"));
        } catch (IllegalArgumentException | NullPointerException e) {
            System.err.println("알 수 없는 카테고리: " + w.get("category_code") + " — 건너뜁니다.");
            continue;
        }

        // ⚠️ occurrences 는 배열이다. 한 단어가 여러 번 나오면 행도 여러 개.
        List<Map<String, Object>> occurrences =
                (List<Map<String, Object>>) w.getOrDefault("occurrences", List.of());

        for (Map<String, Object> occ : occurrences) {
            int slideOrder = ((Number) occ.get("slide_number")).intValue();
            Script script = scriptsBySlideOrder.get(slideOrder);
            if (script == null) continue;      // 그 슬라이드의 대본이 없으면 건너뛴다

            pronunciationHighlightRepository.save(
                PronunciationHighlight.builder()
                    .script(script)
                    .word((String) w.get("word"))
                    .standardPronunciation((String) w.get("standard_pronunciation"))
                    .category(category)
                    .ruleDesc((String) w.get("rule_desc"))
                    .positionStart(((Number) occ.get("position_start")).intValue())
                    .positionEnd(((Number) occ.get("position_end")).intValue())
                    .build());
        }
    }
}
```

**응답 형태:**
```jsonc
{
  "word": "책임",
  "phoneme": "[채김]",
  "standard_pronunciation": "채김",
  "category": "연음",                    // 화면 표시용 (한국어)
  "category_code": "LIAISON",            // ← enum 변환용
  "description": "연음: 앞 받침이 뒤 모음으로 넘어가 발음됩니다.",
  "rule_desc": "연음: 앞 받침이 뒤 모음으로 넘어가 발음됩니다.",
  "occurrences": [
    { "slide_number": 1, "position_start": 4,  "position_end": 6 },
    { "slide_number": 2, "position_start": 11, "position_end": 13 }
  ]
}
```

### ⚠️ 왜 `category_code`인가

```java
HighlightCategory.valueOf(categoryStr.toUpperCase())   // 현재 코드
```
`category`는 **한국어**입니다. 한글에는 대문자가 없어서 `toUpperCase()`를 해도 그대로고, `valueOf("장단음")`은 **항상 예외**를 던집니다. 그래서 catch의 기본값으로 떨어져 **세 카테고리 색이 전부 같아집니다.**

| `category` (화면용) | `category_code` | enum |
|---|---|---|
| 장단음 | `LENGTH` | ✅ |
| 연음 | `LIAISON` | ✅ |
| 표기-발음불일치 | `MISMATCH` | ✅ |

### ⚠️ 좌표 기준

`position_start` / `position_end`는 **그 슬라이드 대본 문자열 기준**입니다(전체 대본 기준 아님).
AI의 `slide_number`는 스프링의 `slideOrder`와 **같은 값**입니다 — `saveScripts()`에서 그렇게 저장하고 계십니다.

### 언제 부르나

**대본이 확정된 뒤 한 번**입니다. `callPythonAiServerForScript()`가 끝난 직후, 그리고 사용자가 대본을 수정해 저장한 뒤에 다시 부르시면 됩니다.

## 5-4. 전체 대본은 `full_script`를 쓰세요

`getFullScriptForRecording()`에서 `"\n\n"`으로 직접 이어붙이고 계신데, AI 서버가 이어붙인 값을 줍니다 — `GET /api/projects/{id}` 응답의 **`full_script`**.

직접 이어붙이셔도 되지만 **그 문자열을 `reference_text`로 되보내지는 마세요.** 슬라이드 사이 빈 줄까지 채점 기준에 들어갑니다.

## 5-5. `getPresentationDetails()` — 썸네일 필드 채우기

```java
// AI 서버 조회 (같은 기계라 빠릅니다 — 실측 0.0초)
HttpHeaders headers = new HttpHeaders();
addApiKeyHeader(headers);

Map ai = new RestTemplate().exchange(
        AI_BASE_URL + "/api/projects/" + presentation.getAiProjectId(),
        HttpMethod.GET, new HttpEntity<>(headers), Map.class).getBody();
Map<String, Object> data = (Map<String, Object>) ai.get("data");

String thumbnailStatus = (String) data.get("thumbnail_status");

Map<Integer, Boolean> hasThumb = new HashMap<>();
for (Map<String, Object> s : (List<Map<String, Object>>) data.get("slides")) {
    hasThumb.put(((Number) s.get("slide_number")).intValue(),
                 Boolean.TRUE.equals(s.get("has_thumbnail")));
}

// SlideScriptDTO 만들 때
.hasThumbnail(hasThumb.getOrDefault(slide.getSlideOrder(), false))

// DetailDTO 만들 때
.thumbnailStatus(thumbnailStatus)
```

---
---

# 6. `PresentationResponseDTO.java`

```java
// DetailDTO
private String thumbnailStatus;     // pending | ready | failed | unavailable | skipped

// SlideScriptDTO
private boolean hasThumbnail;
```

**프론트에 함께 전달해주세요.**

| `thumbnailStatus` | 뜻 | 화면 |
|---|---|---|
| `pending` | 만드는 중 | **2초 간격 폴링** (18장 기준 4초면 끝납니다) |
| `ready` | 완료 | 이미지 표시 |
| `failed` | 변환 실패 | 자리표시자 (재시도 버튼 불필요) |
| `unavailable` | 서버에 변환 도구 없음 | 자리표시자 |
| `skipped` | 대본만 올린 프로젝트 | **정상** — 그릴 슬라이드가 없음 |

⚠️ **`hasThumbnail`이 `true`인 슬라이드만 이미지를 요청**하도록 안내해주세요. 없는 걸 요청하면 404가 오고 브라우저가 **깨진 이미지 아이콘**을 그립니다.

---
---

# 7. `PresentationController.java` — 중계 3종

AI 서버는 `127.0.0.1:8000`으로만 열려 있어 **프론트가 직접 못 부릅니다.** 셋 다 **바이트를 그대로 넘기는** 형태입니다.

```java
/** ① 슬라이드 미리보기 이미지 */
@GetMapping("/api/presentations/{presentationId}/slides/{slideOrder}/thumbnail")
public ResponseEntity<byte[]> getThumbnail(@PathVariable Long presentationId,
                                           @PathVariable int slideOrder) {
    return relay(presentationId,
            "/slides/" + slideOrder + "/thumbnail",
            MediaType.IMAGE_PNG);
}

/** ② 대본 docx */
@GetMapping("/api/presentations/{presentationId}/script.docx")
public ResponseEntity<byte[]> downloadScript(@PathVariable Long presentationId) {
    return relay(presentationId, "/script.docx", null);
}

/** ③ 하이라이팅 docx */
@GetMapping("/api/presentations/{presentationId}/highlight.docx")
public ResponseEntity<byte[]> downloadHighlight(@PathVariable Long presentationId) {
    return relay(presentationId, "/highlight.docx", null);
}

/** AI 서버의 바이트 응답을 그대로 넘긴다. */
private ResponseEntity<byte[]> relay(Long presentationId, String suffix, MediaType contentType) {
    Presentation p = presentationRepository.findById(presentationId)
            .orElseThrow(() -> new IllegalArgumentException("발표 자료를 찾을 수 없습니다."));

    HttpHeaders headers = new HttpHeaders();
    addApiKeyHeader(headers);

    ResponseEntity<byte[]> res = new RestTemplate().exchange(
            AI_BASE_URL + "/api/projects/" + p.getAiProjectId() + suffix,
            HttpMethod.GET, new HttpEntity<>(headers), byte[].class);

    ResponseEntity.BodyBuilder builder = ResponseEntity.ok();
    if (contentType != null) {
        builder.contentType(contentType)
               .cacheControl(CacheControl.maxAge(1, TimeUnit.DAYS).cachePublic());
    } else {
        // docx는 Content-Disposition(파일명)을 그대로 넘겨야 한다
        builder.headers(res.getHeaders());
    }
    return builder.body(res.getBody());
}
```

**실측 크기** — 썸네일 42KB(PNG), 대본 docx 41KB, 하이라이팅 docx 42KB.
docx 파일명은 **프로젝트명**으로 나갑니다.

---
---

# 8. `AiPresentationResponseDto.java` — 지우셔도 됩니다

`presentationId` / `message` 필드를 가지고 있는데 **AI 서버는 그런 필드를 보내지 않습니다.** 확인해보니 **아무 데서도 쓰이지 않는 죽은 코드**입니다.

실제 저장 경로는 `PresentationService.createAiProject()`이고, 그쪽은 정상입니다.
남겨두면 다음에 누가 "이게 파싱하는 줄" 알고 헛짚습니다.

---
---

# 손대실 필요 없는 것 (확인 완료)

| 항목 | 상태 |
|---|---|
| `ai_project_id` 저장 | ✅ 정상. `Map.get("project_id")` → `Number.longValue()`로 안전하게 처리 중 |
| 어투(`Tone`) | ✅ `formal`/`casual` 소문자가 AI 서버와 일치 |
| 점수 타입 | ✅ 엔티티가 `Float`라 정수 `87`이 `87.0f`로 들어감 |
| `WordDetail` | ✅ `reference_span`/`recognized_span`까지 이미 매핑됨 — 평가 결과 하이라이팅은 이것만으로 그릴 수 있음 |
| 부분 녹음 | ✅ 전체 대본을 통째로 녹음하는 구조면 지금 그대로가 맞음 |
| 대본 생성 흐름 | ✅ `/api/projects` → `/api/script/full` → job 폴링 전부 정상 |

---

# 참고 — AI 서버가 주는 것 전체 목록

| 엔드포인트 | 용도 | 응답 |
|---|---|---|
| `POST /api/projects` | 자료·대본 업로드 | JSON (`project_id`) |
| `POST /api/script/full` | 전체 대본 생성 | 202 + `job_id` |
| `GET /api/script/jobs/{id}` | 생성 진행 상태 | JSON |
| `POST /api/script/partial` | 슬라이드 하나 재생성 | JSON |
| `GET /api/projects/{id}` | 상세(대본·주의단어·평가·`full_script`) | JSON |
| `PUT /api/projects/{id}/slides/{n}` | 대본 수정 저장 | JSON |
| `POST /api/analysis/words` | 발음 주의 단어 분석 | JSON |
| `POST /api/evaluation/audio` | 발음 평가 | JSON |
| `POST /api/evaluation/{id}/feedback` | 상세 피드백 | JSON |
| `POST /api/tts/word` | 발음 듣기 | **MP3 바이트** |
| `GET /api/tts/voices` | 화자 목록 | JSON |
| `GET /api/projects/{id}/slides/{n}/thumbnail` | 슬라이드 미리보기 | **PNG 바이트** |
| `GET /api/projects/{id}/script.docx` | 대본 다운로드 | **docx 바이트** |
| `GET /api/projects/{id}/highlight.docx` | 하이라이팅 다운로드 | **docx 바이트** |

모든 호출에 `X-API-Key` 헤더가 필요합니다.
