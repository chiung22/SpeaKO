# DESIGN

## 현재 상태

이 레포에는 UI 코드가 없습니다 (`speako-ai-server`는 백엔드 API 서버만 포함). 따라서 색상/타이포그래피/컴포넌트 규칙 같은 구체적인 디자인 시스템은 아직 정의할 수 없습니다.

프론트엔드가 이 레포(혹은 별도 레포)에 합류하면, 이 문서에 다음을 채워야 합니다.

- 컬러/타이포그래피 토큰
- 공통 컴포넌트 규칙 (버튼, 카드, 폼 등)
- `docs/references/design-system-reference-llms.txt` 같은 외부 디자인 시스템 참조 문서 링크

## API가 이미 암시하는 UX 제약

백엔드 계약만 보고도 프론트엔드 설계에 영향을 주는 지점들이 있어 미리 적어둡니다.

- **대본 생성은 동기 호출이며 시간이 걸릴 수 있습니다.** `/api/script/full`은 HyperCLOVA X 응답을 기다리는 blocking 호출이므로, 로딩 상태 UI가 필수입니다.
- **부분 재생성은 슬라이드 단위입니다.** `target_slide` + `feedback` 텍스트를 받는 구조이므로, UI도 "슬라이드별 재생성" 단위로 상호작용을 설계해야 자연스럽습니다.
- **발음 평가는 오디오 파일 업로드 방식입니다(스트리밍 아님).** 사용자가 녹음을 마친 뒤 파일 전체를 보내는 흐름이므로, 실시간 파형/스트리밍 UI를 기대하면 안 됩니다.
- **모든 API가 안전 모드(mock)를 지원합니다.** 백엔드에 실제 키가 없어도 프론트엔드는 항상 그럴듯한 응답을 받을 수 있으므로, 키 발급을 기다리지 않고 UI 개발을 시작할 수 있습니다.
